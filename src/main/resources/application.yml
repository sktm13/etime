spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/etime?createDatabaseIfNotExist=true
    username: root
    password: dbwlsqlqjs13
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        show_sql: true
        "[format_sql]": true

logging.level:
  "[org.hibernate.SQL]": debug
#  org.hibernate.type: trace

server.servlet.session.tracking-modes: cookie
server.servlet.session.timeout: 1800 # 세션 1800초 글로벌 설정