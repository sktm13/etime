spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/etimev1db?createDatabaseIfNotExist=true
    username: root
    password: dbwlsqlqjs13
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        "[show_sql]": true
        "[format_sql]": true
  servlet:
    multipart:
      max-request-size: 30MB
      max-file-size: 10MB
logging:
  level:
    org:
      springframework:
        security:
          web: trace
eruo:
  v1:
    upload:
      path: upload


